@{
    ViewData["Title"] = "Dashboard";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}



<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-md-6 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Search Houses</h4>
                        <p class="card-description">
                            Search Houses...
                            <h6 style="color:green">@ViewBag.m</h6>
                        </p>
                        <form class="forms-sample" asp-controller="HouseBooking" asp-action="Search" method="post">
                            <div class="form-group">
                                <div class="dropdown">
                                    <label>House Type </label>

                                    <select class="form-control" style="color:black" id="rcity" name="htype">
                                        <option class="dropdown-item" style="color:gray" selected>
                                            Please Select
                                        </option>
                                        @foreach (var item in ViewBag.htype)
                                        {
                                            <option class="dropdown-item">@item</option>
                                        }

                                    </select>

                                </div>

                            </div>

@*                             <div class="form-group">
                                <div class="dropdown">
                                    <label>To </label>

                                    <select class="form-control" style="color:black" id="acity" name="cityto">
                                        <option class="dropdown-item" style="color:grey" selected>
                                            Please Select
                                        </option>
                                        @foreach (var item in ViewBag.acity)
                                        {
                                            <option class="dropdown-item">@item</option>
                                        }

                                    </select>
                                </div>
                            </div> *@
@* 
                            <div class="form-group">
                                <label id="date" class="control-label"></label>
                                <input for="date" type="date" name="date1" class="form-control" />

                            </div> *@

                            <div class="form-group">
                                <div asp-validation-summary="All"></div>
                            </div>
                            <button type="submit" class="btn btn-primary me-2">Search House</button>

                        </form>
                    </div>
                </div>
            </div>


        </div>
    </div>
</div>

